# 匿名书信治愈社区 · 架构设计文档

> 适用范围：对应《项目计划书》「标准 MVP」路线（移动端优先 Web、不开发 App、不开放即时私聊、约 10 周）。
> 配套文档：《技术选型文档》。本文档聚焦系统结构、模块边界、数据模型、核心流程与状态机、安全与运维设计。
> 设计目标：把「一封信从写下 → 寄出 → 被陌生人读到 → 收到回信」的全过程做得**可靠、可审计、安全**，并为 P1/P2 留出演进空间。

---

## 一、架构总览

### 1.1 架构风格

MVP 采用 **模块化单体（Modular Monolith）**：单一后端服务内部按领域分模块，逻辑边界清晰但部署简单。前端、后端 API、异步 Worker、管理后台四类进程，共享一套数据库。

选择理由：低并发、强一致需求、小团队，单体最易交付与维护；模块边界为日后拆分留口。

### 1.2 系统分层图

```
┌─────────────────────────────────────────────────────────────┐
│                          客户端                                │
│   移动端浏览器（主要）        桌面端浏览器          管理员浏览器     │
└───────────────┬───────────────────────────┬──────────────────┘
                │                            │
        ┌───────▼────────┐          ┌────────▼─────────┐
        │  用户端前端       │          │   管理后台前端      │
        │ Next.js (SSR)   │          │ React + AntD     │
        └───────┬────────┘          └────────┬─────────┘
                │  HTTPS / REST(JSON)         │
        ┌───────▼─────────────────────────────▼─────────┐
        │            反向代理 (Nginx/Caddy) + CDN          │
        └───────────────────────┬───────────────────────┘
                                 │
        ┌────────────────────────▼────────────────────────┐
        │                后端 API 服务 (NestJS)              │
        │  Auth │ Letter │ Claim │ Reply │ Mailbox │        │
        │  Conversation │ Report │ Moderation │ Admin │     │
        │  Notification │ Stats │ User/Trust                │
        └───┬───────────────┬───────────────┬──────────────┘
            │               │               │
   ┌────────▼──┐   ┌────────▼─────┐   ┌─────▼────────┐
   │PostgreSQL │   │    Redis     │   │ 对象存储 OSS  │
   │ 主数据     │   │ 缓存/锁/限流  │   │ 素材/导出     │
   │           │   │ /队列(Bull)  │   │              │
   └───────────┘   └──────┬───────┘   └──────────────┘
                          │
                  ┌───────▼────────┐
                  │  异步 Worker     │
                  │ 通知/审核/超时释放 │
                  └───┬─────────┬───┘
                      │         │
              ┌───────▼──┐  ┌───▼──────────┐
              │邮件/短信   │  │内容安全检测     │
              │ 服务      │  │(规则/第三方)   │
              └──────────┘  └──────────────┘

        旁路：Sentry 异常监控 · 云日志 · 每日数据库备份
```

### 1.3 进程构成

| 进程 | 职责 | 是否常驻 |
|---|---|---|
| 用户端前端 (Next.js) | SSR 页面、前端交互、草稿本地暂存 | 是 |
| 管理后台前端 (React/AntD) | 审核、用户管理、举报、内容配置、看板 | 是 |
| API 服务 (NestJS) | 全部业务接口、鉴权、状态机、风控入口 | 是 |
| Worker (BullMQ 消费者) | 通知发送、异步审核、领取超时释放、定时统计 | 是 |

> MVP 可将 API 与 Worker 合并部署（同代码不同启动模式），规模上来后分离。

---

## 二、模块划分（后端领域模块）

| 模块 | 职责 | 关键依赖 |
|---|---|---|
| **Auth** | 注册/登录、验证码、JWT 签发与刷新、会话管理 | Redis（验证码）、User |
| **User/Trust** | 用户资料、匿名昵称、信任分(trust_score)、等级、黑名单 | —— |
| **Letter** | 写信、草稿、投递、信件主题/心情/回信偏好、状态机 | Moderation、Stats |
| **Claim** | 待回信箱分配、领取（抢占锁）、超时释放 | Letter、Redis 锁、Worker |
| **Reply** | 回信编辑、提交、回信质量反馈 | Letter、Claim、Moderation、Notification |
| **Mailbox** | 我的信箱（寄出/收到/回复中/收藏/草稿）聚合查询 | Letter、Reply、Claim |
| **Conversation** | 双方同意后的「往来书信」(P1)，仍以信为单位 | Letter、Reply |
| **Report** | 用户举报、举报分类与优先级 | Moderation |
| **Moderation** | 三层审核管线：自动检测 + 人工审核队列 + 举报处置、审核留痕 | Letter、Reply、内容安全 |
| **Admin** | 管理后台接口、权限、操作审计 | 全部 |
| **Notification** | 站内通知 + 邮件/短信（回信到达、审核结果等） | Worker、邮件/短信服务 |
| **Stats** | 数据看板指标（北极星 + 写信/回信/留存/安全指标） | 读模型/定时聚合 |

**模块边界原则**：跨模块只通过 Service 接口调用，不直接读对方的表；状态变更统一经由领域服务，便于在一处插入审核与审计。

---

## 三、核心数据模型

基于计划书第十三章核心数据表，补全字段、状态与索引建议。统一约定：主键用 `id`（雪花/UUID），时间字段 UTC 存储。

### 3.1 users 用户表
| 字段 | 类型 | 说明 |
|---|---|---|
| user_id | PK | 稳定内部标识（对外不暴露） |
| nickname | varchar | 匿名昵称，可改 |
| account_type | enum | email / phone |
| email / phone | varchar | 二选一，唯一索引，加密/脱敏存储 |
| status | enum | active / write_limited / reply_limited / banned / deleted |
| trust_score | int | 信任分，驱动领取额度与等级 |
| interested_topics | jsonb | 感兴趣主题 |
| blocked_topics | jsonb | 不想看到的主题 |
| created_at / last_active_at | timestamptz | |

索引：`email`/`phone` 唯一；`status`。

### 3.2 letters 信件表
| 字段 | 类型 | 说明 |
|---|---|---|
| letter_id | PK | |
| author_id | FK→users | |
| title | varchar null | 选填 |
| content | text | 正文（纯文本/有限格式） |
| category | enum | 学业/工作/感情/家庭/友情/自我成长/孤独/迷茫/遗憾/生活故事/只是想说说/其他 |
| mood | enum null | 当前心情 |
| reply_preference | jsonb | 希望的回复类型、是否允许多人回信、收到首封后是否关闭领取 |
| is_public | bool | 是否匿名公开展示 |
| max_replies | int | 默认 1~3 |
| status | enum | 见 §4 状态机 |
| risk_level | enum | none / low / high |
| created_at / published_at | timestamptz | |

索引：`(status, category)` 用于待回信箱分配；`author_id`；`risk_level`。

### 3.3 letter_claims 领取记录表
| 字段 | 类型 | 说明 |
|---|---|---|
| claim_id | PK | |
| letter_id | FK | |
| reader_id | FK→users | |
| claimed_at / expires_at | timestamptz | 领取时限 24/48h |
| status | enum | claimed / replied / expired / released |

约束：`(letter_id, reader_id)` 唯一（同人不重复领取）；活跃领取数受 `max_replies` 限制。

### 3.4 replies 回信表
| 字段 | 类型 | 说明 |
|---|---|---|
| reply_id | PK | |
| letter_id | FK | |
| claim_id | FK | 关联领取 |
| writer_id | FK→users | |
| content | text | |
| status | enum | draft / submitted / published / rejected |
| risk_level | enum | none / low / high |
| feedback | enum null | 被理解/认真/有帮助/一般/不舒服（不公开，仅用于质量评分） |
| created_at | timestamptz | |

### 3.5 conversations 往来关系表（P1）
| 字段 | 类型 | 说明 |
|---|---|---|
| conversation_id | PK | |
| user_a / user_b | FK | |
| source_letter_id | FK | 由哪封信发起 |
| status | enum | pending / active / ended |
| created_at | timestamptz | |

往来仍以「一封信」为单位（复用 letters/replies），不显示在线/正在输入。

### 3.6 reports 举报表
| 字段 | 类型 | 说明 |
|---|---|---|
| report_id | PK | |
| reporter_id | FK | |
| target_type | enum | letter / reply / user |
| target_id | bigint | |
| reason | enum | 骚扰/人身攻击/色情/广告诈骗/索要联系方式/危险建议/泄露隐私/其他 |
| description | text null | |
| priority | enum | low / normal / high |
| status | enum | pending / processing / resolved / rejected |
| handled_by / handled_at | | |

### 3.7 moderation_records 审核记录表
| 字段 | 类型 | 说明 |
|---|---|---|
| record_id | PK | |
| target_type / target_id | | letter / reply |
| auto_result | jsonb | 命中规则、风险词、敏感信息位置 |
| action | enum | pass / mask_pass / return / reject / limit / ban / escalate |
| operator_id | null=系统 | 系统自动 or 审核员 |
| note | text | |
| created_at | | |

**全程留痕，保证可追溯**（计划书要求）。

### 3.8 drafts / notifications / audit_logs（补充）
- `drafts`：服务端草稿备份（前端 localStorage 为主，服务端兜底）。
- `notifications`：站内通知（类型、目标用户、已读状态）。
- `audit_logs`：所有管理员操作审计（谁、何时、对什么、做了什么）。

---

## 四、核心流程与状态机

### 4.1 信件状态机（产品闭环的核心）

```
        ┌─────────┐  封好投递   ┌──────────┐ 自动+人工审核
draft──▶│submitted│──────────▶│under_     │
        └─────────┘            │moderation│
                               └────┬─────┘
                    审核通过/遮挡放行 │ │ 审核拒绝
                                    │ └──────────▶ rejected
                                    ▼
                            ┌──────────────┐
                            │waiting_claim │◀───────────┐
                            │(等待领取)      │  领取超时    │
                            └──────┬───────┘  自动释放     │
                          被领取(抢占锁)│                  │
                                    ▼                    │
                            ┌──────────────┐             │
                            │  claimed     │─────────────┘
                            │(已领取/回信中) │
                            └──────┬───────┘
                          回信提交并通过审核│
                                    ▼
                            ┌──────────────┐
                            │  replied     │
                            │(已收到回信)    │
                            └──────┬───────┘
                       达到 max_replies / │
                       作者关闭 / 双方结束   │
                                    ▼
                            ┌──────────────┐
                            │   closed     │
                            └──────────────┘
```

要点：
- 若 `max_replies > 1` 且未关闭领取，`replied` 后可回到 `waiting_claim` 继续接受领取。
- 任意状态可因举报/审核进入 `rejected` 或被隐藏。

### 4.2 写信 → 投递流程
1. 用户在写信页编辑，前端**防抖自动保存**到 localStorage + 周期性调用草稿接口。
2. 点击「封好信件」→ 前端做敏感信息预检与提示。
3. 提交到 `POST /letters`：服务端校验最低字数、防重复提交（幂等键）、落库 `submitted`。
4. 同步触发**自动检测**（正则联系方式/敏感词/风险词）：
   - 无风险 → 直接 `waiting_claim`（或先 `under_moderation` 抽查）。
   - 命中高风险 → `under_moderation`，入人工审核队列。
5. 投递动画（1~2s，纯前端）。

### 4.3 领取流程（并发抢占，难点）
```
回信者请求「读一封信」
   → 分配候选：按 category 偏好 + 风险等级 + 排除已跳过/已领取 + 排除自己写的
   → 事务内：SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1（跳过被锁的信）
   → 写入 letter_claims(claimed, expires_at=now+24/48h)
   → 校验未超过该信 max_replies、回信者每日领取额度
   → 提交事务；Redis 设锁兜底防并发
   → 投递 BullMQ 延迟任务：到期检查是否完成，未完成则释放回 waiting_claim
```
- **每次只出现一封信**（计划书要求），不展示大量列表。
- 跳过的信短期内不再分配给同一用户（Redis 记录）。

### 4.4 回信流程
1. 进入回信页，展示原信内容 + 温和引导提示。
2. 草稿自动保存。
3. 提交 `POST /replies`：关联 `claim_id`，状态 `submitted` → 自动检测 →（高风险）人工审核 → `published`。
4. 发布后投递**通知任务**：原作者收到「你的信箱里有一封新来信」（不直接显示内容，点击信封再展开，增强仪式感）。
5. 更新领取状态为 `replied`，推进信件状态机。

### 4.5 审核管线（三层，与写信同期建设）
```
内容提交 → [第一层 自动检测]
              命中联系方式 → 自动遮挡 + 提示
              命中高风险词  → 标记 high，强制入队
              ↓ 低/无风险
           [抽查/直接放行]
              ↓ 高风险
           [第二层 人工审核队列] 审核员：放行/遮挡放行/退回/拒绝/限制/封禁/升级
              ↓
           [第三层 用户举报] 任何已发布内容可被举报 → 进入同一处置后台
                   ↓ 所有动作写 moderation_records / audit_logs
```

### 4.6 通知机制（无即时通讯）
- 站内通知 + 邮件提醒，经 BullMQ 异步发送。
- 不做 WebSocket / 在线状态 / 正在输入（违背产品定位）。
- 「收到回信」类通知只提示有新信，不剧透内容。

---

## 五、安全与隐私架构（计划书第十、十一章）

| 维度 | 设计 |
|---|---|
| **匿名 vs 可追溯** | 对外匿名昵称；对内稳定 `user_id` 用于风控、举报、信任记录 |
| **敏感信息** | 提交前前端高亮提示 + 后端正则强制遮挡（手机号/微信/邮箱/住址/身份证/银行卡/学校班级/公司部门） |
| **传输/存储** | 全站 HTTPS；邮箱/手机号加密或脱敏存储；密码（如有）bcrypt/argon2 |
| **会话安全** | JWT 短时 + Refresh Token（HttpOnly+Secure Cookie）；CSRF 防护；SameSite |
| **限流/防刷** | Redis 限流：注册、验证码、投递、领取、举报各设阈值；新用户每日投递 1 封、领取 1~2 封 |
| **重复注册** | 邮箱/手机号唯一 + IP/设备指纹风控 |
| **高风险情绪内容** | 自伤/轻生风险词专门识别 → 特殊审核队列 + 向用户展示紧急求助提示；不让普通回信者承担救助责任 |
| **未成年人** | MVP 仅对成年人开放（最稳妥），预留年龄识别与更严限制的扩展点 |
| **数据权利** | 提供账户注销 + 数据导出/删除申请入口与流程 |
| **产品边界声明** | 全站显著声明：非专业心理咨询，紧急情况联系现实专业服务 |
| **合规** | 面向中国大陆运营前完成备案、隐私政策、内容治理、用户协议，由专业人员复核 |

---

## 六、管理后台架构（计划书第十二章）

独立 React + AntD 前端，复用部分 API，独立管理员权限体系（RBAC）。模块：

1. **数据看板**：新增/活跃用户、新增/已回复/未回复信件、平均等待时间、举报数、高风险数、回信完成率、北极星（每周有效书信往来数）。
2. **信件审核**：正文 + 风险标签 + 自动检测结果 + 敏感字段修改 + 放行/拒绝 + 操作记录 + 二次复核。
3. **用户管理**：行为记录、举报记录、限制写信/回信、临时/永久封禁、申诉处理。
4. **举报管理**：分类、优先级、处理人、处理结果、处理时长、复核记录。
5. **内容配置**：首页文案、信件主题、社区规则、系统通知、活动信箱、风险提示语。

所有管理操作写 `audit_logs`，可追溯。

---

## 七、关键技术要求落地（计划书十三章 §3）

| 要求 | 实现方式 |
|---|---|
| 草稿自动保存 | 前端防抖 localStorage + 服务端草稿接口兜底 |
| 防止重复提交 | 幂等键（请求携带 client token）+ 唯一约束 |
| 信件领取锁 | PG `FOR UPDATE SKIP LOCKED` + Redis 分布式锁兜底 |
| 领取超时自动释放 | BullMQ 延迟任务 + 定时扫描兜底，幂等释放 |
| 内容版本记录 | 信件/回信内容变更与审核遮挡留版本与审核记录 |
| 敏感信息识别 | 规则引擎 + 正则；高风险送审或调云内容安全 |
| 用户操作限流 | Redis 计数限流，分接口配置 |
| 账户/IP 风险控制 | 风控规则 + 黑名单 + 设备/IP 维度统计 |
| 数据库每日备份 | 托管 PG 自动快照 + 定期恢复演练 |
| 管理员操作审计 | audit_logs 全量记录 |
| 关键异常报警 | Sentry + 云日志告警 |
| 移动端性能优化 | SSR、按需加载、图片极少、长文排版优化 |

---

## 八、部署与环境

- **环境**：dev / staging / prod 三套；CI 自动构建 Docker 镜像。
- **拓扑**：Nginx/Caddy（反代+TLS）→ Next.js + NestJS + Worker（容器）→ 托管 PostgreSQL/Redis → 对象存储 → CDN。
- **可观测性**：Sentry（异常）+ 云日志（访问/业务/审计）+ 看板（业务指标）。
- **备份与恢复**：每日数据库快照，定期演练恢复（MVP 验收项）。
- **扩展路径**：先垂直扩容；读多写少时为 Stats 增加只读副本/读模型；Worker 与 API 分离部署；必要时再考虑模块拆服务。

---

## 九、演进规划（与 P1/P2 对应）

| 阶段 | 架构动作 |
|---|---|
| **MVP (P0)** | 模块化单体跑通写信/领取/回信/信箱/审核/后台闭环 |
| **P1** | 启用 Conversation 模块（往来书信，仍走信件单位）；信纸样式走对象存储+配置；信誉等级由 trust_score 计算；邮件提醒增强 |
| **P2** | 未来信（BullMQ 定时投递）；声音来信（对象存储+转码）；多语言 i18n；信件分配从规则升级为推荐/模型；高负载时 Worker/读模型分离 |

演进遵循「**只做加法、不推倒重来**」：P0 的数据模型与模块边界已为后续预留扩展点。

---

## 十、小结

本架构围绕产品真正的竞争力——**用户愿意写、能收到认真回复、感到安全**——做了三处重点设计：

1. **可靠的信件状态机 + 领取抢占机制**：保证「一封信不被重复领取、超时可回收、状态清晰」。
2. **与写信同期的三层审核管线**：把内容安全做成系统的一等公民，而非事后补丁。
3. **匿名但可追溯的账户与风控体系**：在保护隐私的同时让平台可治理。

技术上保持克制（单体、纯文本、无 IM、无过度算法），把复杂度投入到真正重要的地方，契合「一个安静的、只有写信/读信/回信/信箱四个核心动作的网站」这一第一版定位。
