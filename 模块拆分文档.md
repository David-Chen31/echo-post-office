# 匿名书信治愈社区 · 模块拆分文档

> 适用范围：「标准 MVP」路线。本文档把系统拆成**可独立分工、可照着建目录、可照着写接口**的模块，是写代码前的最后铺垫。
> 配套：《架构设计文档》《数据模型文档》《技术选型文档》。
> 形态：模块化单体——一个 NestJS 后端按领域分模块；一个 Next.js 用户端；一个 React/AntD 管理后台；一个共享包。

---

## 一、代码仓库与工程结构

采用 **monorepo（pnpm workspace）**，前后端共享类型与枚举：

```
letter-community/
├─ package.json                # pnpm workspace 根
├─ pnpm-workspace.yaml
├─ docker-compose.yml          # 本地：postgres + redis
├─ apps/
│  ├─ api/                     # NestJS 后端（含 Worker）
│  ├─ web/                     # Next.js 用户端（移动优先）
│  └─ admin/                   # React + AntD 管理后台
├─ packages/
│  ├─ shared/                  # 共享：枚举、DTO 类型、常量、校验 schema(zod)
│  └─ config/                  # 共享 eslint/tsconfig/tailwind preset
└─ prisma/
   ├─ schema.prisma            # 见《数据模型文档》§4
   └─ seed.ts                  # 冷启动种子
```

> 早期人手少也可不用 monorepo，但**至少把枚举与 DTO 类型抽成共享包**，避免前后端漂移。

---

## 二、后端模块总览（NestJS）

```
apps/api/src/
├─ main.ts
├─ app.module.ts
├─ common/                     # 横切关注点
│  ├─ guards/                  # JwtAuthGuard, RolesGuard, RateLimitGuard
│  ├─ interceptors/            # 审计、日志、响应包装
│  ├─ filters/                 # 全局异常 → Sentry
│  ├─ pipes/                   # zod 校验管道
│  └─ decorators/              # @CurrentUser, @Roles, @Idempotent
├─ infra/                      # 基础设施封装
│  ├─ prisma/                  # PrismaService
│  ├─ redis/                   # RedisService（锁/限流/缓存）
│  ├─ queue/                   # BullMQ 队列定义
│  ├─ mail/                    # 邮件/短信适配
│  └─ storage/                 # 对象存储适配
└─ modules/
   ├─ auth/
   ├─ user/
   ├─ letter/
   ├─ claim/
   ├─ reply/
   ├─ mailbox/
   ├─ conversation/   (P1)
   ├─ report/
   ├─ moderation/
   ├─ notification/
   ├─ stats/
   ├─ admin/
   └─ worker/                  # 队列消费者 + 定时任务
```

每个领域模块的标准内部结构：
```
modules/<name>/
├─ <name>.module.ts
├─ <name>.controller.ts        # 用户端接口
├─ <name>.service.ts           # 业务逻辑（状态机/规则）
├─ <name>.repository.ts        # Prisma 数据访问（仅本模块表）
├─ dto/                        # 请求/响应 DTO（引用 shared 的 zod schema）
└─ <name>.spec.ts             # 单元测试
```

**铁律**：跨模块只能调用对方的 `Service`，禁止跨模块直接访问对方 `Repository`/表。

---

## 三、各模块职责与依赖

| 模块 | 拥有的表 | 核心职责 | 依赖（调用的 Service） |
|---|---|---|---|
| **auth** | （读 users）+ Redis 验证码 | 注册/登录、验证码、JWT 签发/刷新、登出 | user |
| **user** | users, user_blocks | 资料、昵称、信任分、等级派生、黑名单、限额、注销 | —— |
| **letter** | letters, letter_drafts | 写信、草稿、投递、状态机推进、信件查询 | moderation, notification |
| **claim** | letter_claims | 待回信箱分配、领取抢占、跳过、超时释放触发 | letter, user, queue |
| **reply** | replies, reply_feedbacks | 回信草稿/提交、发布、质量反馈 | letter, claim, moderation, notification |
| **mailbox** | （聚合读） | 我的信箱：寄出/收到/回复中/收藏/草稿 聚合查询 | letter, reply, claim |
| **conversation**(P1) | conversations | 申请继续通信、双方同意、往来（仍走信件单位） | letter, reply, notification |
| **report** | reports | 用户举报、分类、优先级 | moderation |
| **moderation** | moderation_records, sensitive_hits | 三层审核：自动检测、人工审核队列、举报处置、留痕 | （被 letter/reply/report 调用） |
| **notification** | notifications | 站内通知 + 邮件/短信派发 | queue, mail |
| **stats** | daily_stats | 看板指标聚合（定时） | —— |
| **admin** | （跨模块只读+受控写）audit_logs, content_configs | 后台所有接口、RBAC、操作审计、内容配置 | 全部模块 Service |
| **worker** | —— | 队列消费 + 定时任务 | claim, moderation, notification, stats |

依赖方向自上而下，**禁止环依赖**。`moderation`、`notification` 为被依赖的底层服务；`admin` 为顶层编排。

---

## 四、模块接口契约（关键 Service 方法签名）

> 用 TS 表达，供分工时对齐。完整 DTO 见 `packages/shared`。

### auth
```ts
register(dto: RegisterDto): Promise<{ userId: bigint }>          // 邮箱/手机号 + 验证码
sendCode(target: string, scene: 'register'|'login'): Promise<void>
login(dto: LoginDto): Promise<TokenPair>                          // access + refresh
refresh(refreshToken: string): Promise<TokenPair>
logout(userId: bigint): Promise<void>
```

### letter
```ts
saveDraft(userId: bigint, dto: DraftDto): Promise<DraftView>
submit(userId: bigint, dto: SubmitLetterDto, idemKey: string): Promise<LetterView>
//   内部：校验字数/限额/防重复 → 落库 SUBMITTED → 调 moderation.autoCheck → 置状态
getById(viewerId: bigint, letterId: bigint): Promise<LetterView>  // 脱敏：模糊时间、隐藏作者
advanceState(letterId: bigint, event: LetterEvent): Promise<void> // 状态机唯一入口
close(userId: bigint, letterId: bigint): Promise<void>
```

### claim（抢占核心）
```ts
pickOneForReader(readerId: bigint): Promise<LetterView | null>    // §数据模型 §5.3 抢占 SQL
skip(readerId: bigint, letterId: bigint): Promise<void>           // Redis 记录跳过
releaseExpired(): Promise<number>                                 // Worker 调用，幂等
assertClaimable(readerId: bigint): Promise<void>                  // 每日领取额度校验
```

### reply
```ts
saveDraft(writerId: bigint, claimId: bigint, content: string): Promise<void>
submit(writerId: bigint, claimId: bigint, content: string, idemKey: string): Promise<ReplyView>
//   内部：校验领取有效/未超时 → moderation.autoCheck → PUBLISHED → notification + claim.markReplied
giveFeedback(raterId: bigint, replyId: bigint, type: ReplyFeedbackType): Promise<void>
```

### moderation
```ts
autoCheck(target: ModTarget): Promise<{ riskLevel: RiskLevel; hits: SensitiveHit[] }>
//   正则联系方式 + 敏感词库 + 风险词；命中 HIGH → 入人工队列
enqueueManual(target: ModTarget): Promise<void>
review(operatorId: bigint, target: ModTarget, action: ModerationAction, note?: string): Promise<void>
maskSensitive(content: string, hits: SensitiveHit[]): string      // 遮挡联系方式
```

### notification
```ts
push(userId: bigint, type: NotificationType, payload: object): Promise<void>  // 入队异步发
listUnread(userId: bigint): Promise<NotificationView[]>
markRead(userId: bigint, ids: bigint[]): Promise<void>
```

### mailbox
```ts
sent(userId): Promise<LetterView[]>            // 我寄出的（含状态）
received(userId): Promise<ReplyView[]>         // 我收到的回信
replying(userId): Promise<ClaimView[]>         // 我正在回复的
favorites(userId): Promise<ReplyView[]>        // 收藏
drafts(userId): Promise<DraftView[]>
```

---

## 五、后端 REST API 清单（用户端）

> 统一前缀 `/api/v1`，鉴权走 `JwtAuthGuard`（除登录/注册/公开页）。响应统一 `{ code, data, message }`。

| 方法 | 路径 | 说明 | 模块 |
|---|---|---|---|
| POST | `/auth/code` | 发送验证码 | auth |
| POST | `/auth/register` | 注册 | auth |
| POST | `/auth/login` | 登录 | auth |
| POST | `/auth/refresh` | 刷新 token | auth |
| POST | `/auth/logout` | 登出 | auth |
| GET | `/me` | 当前用户资料 | user |
| PATCH | `/me` | 改昵称/通知/主题偏好 | user |
| POST | `/me/blocks` | 拉黑用户 | user |
| DELETE | `/me` | 申请注销/删除数据 | user |
| POST | `/letters/draft` | 保存草稿 | letter |
| POST | `/letters` | 投递信件（幂等头 `Idempotency-Key`） | letter |
| GET | `/letters/:id` | 读信详情（脱敏） | letter |
| POST | `/letters/:id/close` | 关闭领取/结束 | letter |
| GET | `/mailbox/inbox` | 待回信箱：取一封可领取的信 | claim |
| POST | `/mailbox/inbox/:id/claim` | 领取这封信 | claim |
| POST | `/mailbox/inbox/:id/skip` | 跳过 | claim |
| POST | `/replies/draft` | 回信草稿 | reply |
| POST | `/replies` | 提交回信（幂等） | reply |
| POST | `/replies/:id/feedback` | 回信质量反馈 | reply |
| POST | `/replies/:id/favorite` | 收藏回信 | mailbox |
| GET | `/mailbox/sent` | 我寄出的信 | mailbox |
| GET | `/mailbox/received` | 我收到的回信 | mailbox |
| GET | `/mailbox/replying` | 我正在回复的 | mailbox |
| GET | `/mailbox/drafts` | 我的草稿 | mailbox |
| POST | `/reports` | 举报 | report |
| GET | `/notifications` | 未读通知 | notification |
| POST | `/notifications/read` | 标记已读 | notification |
| GET | `/configs/:key` | 公开内容配置（首页文案/规则） | admin(只读) |

### 管理后台 API（前缀 `/api/v1/admin`，`RolesGuard`）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/admin/dashboard` | 数据看板指标 |
| GET | `/admin/letters?status&risk` | 信件审核队列 |
| POST | `/admin/letters/:id/review` | 审核动作（放行/遮挡/退回/拒绝） |
| GET | `/admin/replies?...` | 回信审核 |
| GET | `/admin/reports?status&priority` | 举报队列 |
| POST | `/admin/reports/:id/handle` | 处理举报 |
| GET | `/admin/users/:id` | 用户行为/举报记录 |
| POST | `/admin/users/:id/limit` | 限制写信/回信 |
| POST | `/admin/users/:id/ban` | 临时/永久封禁 |
| POST | `/admin/users/:id/appeal` | 处理申诉 |
| GET/PUT | `/admin/configs/:key` | 内容配置 |
| GET | `/admin/audit-logs` | 操作审计查询 |

---

## 六、前端模块拆分

### 6.1 用户端（apps/web，Next.js App Router，移动优先）
```
app/
├─ (public)/page.tsx              # 首页：写一封信 / 读一封信
├─ (public)/rules/page.tsx        # 社区规则 & 帮助中心
├─ write/page.tsx                 # 写信页（信纸编辑、主题/心情/偏好、自动保存）
├─ write/sent/page.tsx            # 投递动画(1~2s) + 完成
├─ read/page.tsx                  # 待回信箱：每次一封
├─ read/[id]/page.tsx             # 阅读信件（领取按钮、举报）
├─ reply/[claimId]/page.tsx       # 回信编辑（原信 + 引导提示 + 草稿）
├─ mailbox/                       # 我的信箱（寄出/收到/回复中/收藏/草稿）
│  └─ received/[id]/page.tsx      # 拆信：先信封后正文（仪式感）
├─ settings/page.tsx              # 个人设置
└─ auth/                          # 登录/注册
components/                       # Envelope, LetterPaper, Stamp, Mailbox 等拟物组件
lib/                              # api client, 草稿本地存储(localStorage), 敏感词前端预检
```
关键交互（计划书要求）：草稿自动保存、键盘弹起编辑区正常、返回不丢内容、收回信先信封后正文、模糊时间展示。

### 6.2 管理后台（apps/admin，React + AntD）
```
src/pages/
├─ Dashboard/        # 看板
├─ LetterReview/     # 信件审核（风险标签、敏感命中高亮、遮挡放行）
├─ ReplyReview/
├─ Reports/          # 举报处理
├─ Users/            # 用户管理、封禁、申诉
├─ Configs/          # 内容配置
└─ AuditLogs/
```

### 6.3 共享包（packages/shared）
- `enums.ts`：§数据模型 §2 所有枚举，前后端唯一来源。
- `dto/`：zod schema + 推导类型（注册、投递、回信、举报…），前端校验 + 后端管道复用。
- `constants.ts`：每日限额默认值、领取时限、最低字数、主题文案 key。

---

## 七、横切关注点（common / infra）

| 关注点 | 实现 | 落点 |
|---|---|---|
| 鉴权 | JwtAuthGuard + @CurrentUser | common/guards |
| 权限 | RolesGuard + @Roles('admin'/'moderator') | common/guards |
| 限流 | RateLimitGuard（Redis 计数，按接口配额） | common/guards |
| 幂等 | @Idempotent（Idempotency-Key + Redis） | common/decorators |
| 校验 | ZodValidationPipe | common/pipes |
| 审计 | AuditInterceptor（admin 写操作 → audit_logs） | common/interceptors |
| 异常 | AllExceptionsFilter → Sentry | common/filters |
| 分布式锁 | RedisService.lock(key, ttl) | infra/redis |
| 队列 | BullMQ：notification / moderation / claim-expire / stats | infra/queue |

---

## 八、Worker 任务清单（apps/api worker 模式）

| 任务 | 触发 | 职责 |
|---|---|---|
| `notification.send` | 事件入队 | 发站内/邮件/短信通知 |
| `moderation.autoCheck` | 投递/回信提交入队（或同步） | 异步跑风险检测，高风险入人工队列 |
| `claim.releaseExpired` | 延迟任务 + 定时(每分钟)兜底 | 超时领取置 EXPIRED，信件回 WAITING_CLAIM（幂等） |
| `stats.aggregateDaily` | 定时(每日) | 聚合 daily_stats（含北极星指标） |
| `letter.scheduledDeliver`(P2) | 延迟任务 | 未来信定时投递 |

---

## 九、开发分工建议（对应 10 周排期第 5~7 周核心开发）

| 并行轨 | 模块 | 负责人建议 |
|---|---|---|
| 轨 A：写信链路 | auth, user, letter, claim, reply, mailbox | 后端主力 |
| 轨 B：治理链路 | moderation, report, notification, admin, worker | 后端次力 / 全栈 |
| 轨 C：用户端 | apps/web 全部页面 + 拟物组件 | 前端 |
| 轨 D：后台 | apps/admin | 前端 / 全栈 |
| 贯穿 | packages/shared, infra, common | 技术负责人先搭，全员共用 |

**联调里程碑**：先打通「注册 → 写信投递 → 领取 → 回信 → 收到通知拆信」最小闭环（端到端），再补治理、限额、统计。

---

## 十、开工检查清单（Definition of Ready）

写第一行业务代码前确认：
- [ ] monorepo 脚手架 + `docker-compose`（pg/redis）可本地起。
- [ ] `prisma migrate` 跑通，`packages/shared` 枚举与 DB 一致。
- [ ] common/infra（鉴权、限流、幂等、异常、队列、锁）骨架就位。
- [ ] API 响应格式、错误码规范、`Idempotency-Key` 约定统一。
- [ ] 模块边界纪律达成共识（跨模块只调 Service）。
- [ ] 冷启动种子脚本预留（示范信/回信/配置）。

完成后即可按 §九 分工进入核心功能开发。
