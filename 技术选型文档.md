# 匿名书信治愈社区 · 技术选型文档

> 适用范围：本文档对应《项目计划书》中推荐的「标准 MVP」路线——移动端优先的网站、不开发 App、不开放即时私聊、约 10 周完成第一版。
> 目标读者：项目负责人、全栈开发、外包评估方。
> 选型原则：**够用、稳定、可被一两个全栈开发者维护、能平滑长到 P1/P2**，而不是追求技术先进性。

---

## 一、选型总原则

书信产品的技术特征决定了选型方向：

1. **读写都不是高并发**。用户「认真写一封信」「认真读一封信」，QPS 很低，瓶颈在内容安全和运营，而不在性能。
2. **强一致性比高吞吐重要**。一封信不能被两个人同时领取、不能重复回复、领取超时要可靠释放——这些是事务和锁的问题。
3. **内容安全是第一公民**。审核、风险词、敏感信息识别要和写信功能同期建设，技术栈必须方便插入审核环节。
4. **团队小**。4~7 人，全栈 1~2 人，应避免多语言、多框架、需要专职运维的方案。
5. **可演进**。MVP 用单体即可，但数据模型和模块边界要为后续拆分留余地。

据此，整体采用 **TypeScript 全栈单体 + PostgreSQL + Redis** 的保守组合。

---

## 二、技术选型总表

| 层 | 选型 | 版本建议 | 替代方案 | 选择理由 |
|---|---|---|---|---|
| 前端框架 | Next.js (App Router) + React | Next 14/15 LTS、React 18+ | Nuxt、Remix | 计划书指定；SSR/SEO 友好，移动端首屏快；与后端同语言 |
| 语言 | TypeScript | 5.x | JavaScript | 类型安全，信件/状态机字段多，强类型可减少 bug |
| 样式 | Tailwind CSS | 3.x | CSS Modules、UnoCSS | 计划书指定；留白、低信息密度的视觉用工具类快速实现 |
| 编辑器 | 受控 textarea + 轻量富文本（仅分段/加粗/引用/分隔线） | Tiptap（如需） | 复杂富文本（不推荐） | 计划书要求纯文本+有限格式，避免图片/视频/外链 |
| 后端 | NestJS（独立服务） | 10.x | Next.js API Routes（更早期） | 有清晰模块/依赖注入/守卫，适合状态机、权限、审核管线 |
| 主数据库 | PostgreSQL | 15/16 | MySQL 8 | 事务、行锁、JSONB、`SELECT FOR UPDATE SKIP LOCKED` 适合「领取」抢占 |
| ORM | Prisma | 5.x | TypeORM、Drizzle | 类型安全、迁移友好、与 TS 配合好 |
| 缓存/限流/队列 | Redis | 7.x | —— | 验证码、限流、领取锁、分布式锁、轻量队列 |
| 任务队列 | BullMQ（基于 Redis） | 最新 | RabbitMQ（过重）、独立 MQ | 通知、审核任务、超时释放定时任务，无需引入独立 MQ |
| 管理后台 | React + Ant Design（独立前端应用） | AntD 5.x | Refine、低代码平台 | 计划书指定；表格/表单/审核流天然契合 |
| 对象存储 | 云厂商 OSS / S3 兼容 | —— | 本地磁盘（不推荐） | 静态资源、信纸素材、数据导出文件 |
| 邮件/短信 | 云厂商邮件 + 短信服务 | —— | 自建 SMTP（不推荐） | 注册验证、回信通知 |
| 鉴权 | 自建 JWT + Refresh Token（HttpOnly Cookie） | —— | Auth.js/NextAuth、第三方 IdP | 匿名社区需自控账户标识与风控，不依赖外部身份 |
| 监控 | Sentry + 云日志 | —— | 自建 ELK（过重） | 异常报警、关键流程追踪 |
| 部署 | 云服务器 + 容器（Docker）+ 反向代理 | —— | Serverless（早期慎用） | 成本可控、便于备案、状态机/队列需要常驻进程 |

---

## 三、关键选型决策说明

### 1. 为什么后端用独立的 NestJS，而不只用 Next.js API Routes？

计划书提到「Next.js API 用于早期 MVP」是可行的，但本产品的核心是**带状态机和审核管线的业务逻辑**：

- 信件状态机（已投递→等待领取→已领取→回信中→已回信→已结束）
- 领取锁、超时释放定时任务、队列消费
- 三层审核（自动检测 / 人工审核 / 用户举报）
- 管理后台与用户端共享业务但权限不同

这些用 NestJS 的模块、Provider、Guard、Pipe、Schedule、Queue 表达更清晰，也更容易让外包交付物可审计。

**折中建议**：若团队只有 1 名全栈、且追求最低成本（方案 A 低成本原型），可先用 Next.js API Routes 起步，但务必保持「业务逻辑独立于路由层」，便于后续抽到 NestJS。

### 2. 为什么是 PostgreSQL 而不是 MySQL？

「领取一封信」本质是**并发抢占**：多个回信者看到同一封信，只能有一个领取成功。PostgreSQL 的 `SELECT ... FOR UPDATE SKIP LOCKED` 能优雅实现「跳过已被锁定的信、原子领取下一封可用信」，避免大量锁等待。配合 JSONB（存风险标签、审核结果）、丰富的索引类型，更契合本项目。MySQL 8 亦可，但 PG 在此场景更顺手。

### 3. 编辑器：坚决克制

计划书明确「正文优先纯文本 + 有限格式，不支持图片/视频/外链」。技术上：

- 优先用**受控 textarea + 自动保存**，格式仅做加粗/引用/分隔线的轻量标记。
- 若确需富文本，用 Tiptap 并**白名单严格限制**节点类型，禁用图片/链接/HTML 粘贴。
- 富文本必须在后端做**二次清洗（sanitize）**，前端格式不可信。

### 4. 鉴权：自建而非第三方登录

匿名社区的核心是「对外匿名、对内有稳定账户标识用于风控」（计划书十一章）。因此：

- 注册用邮箱或手机号 + 验证码，落库为稳定 `user_id`。
- 会话用 JWT（短时 access）+ Refresh Token（HttpOnly、Secure Cookie），防 XSS 窃取。
- 不接第三方社交登录（避免引入社交身份，违背匿名定位）。

### 5. 任务队列：BullMQ 起步，不上独立 MQ

计划书提到「消息队列处理通知和审核任务」。MVP 阶段引入 RabbitMQ/Kafka 属于过度设计。**Redis + BullMQ** 即可覆盖：

- 回信/审核结果通知（邮件/站内）
- 自动审核任务（异步跑风险词/敏感信息检测）
- 领取超时释放（延迟任务 / 定时扫描）

规模上来后若队列成为瓶颈，再迁移到专用 MQ。

### 6. 内容安全：分层落地

| 层级 | 技术手段 | MVP 实现 |
|---|---|---|
| 自动检测 | 正则 + 敏感词库 + 规则引擎 | 手机号/微信号/邮箱/地址正则、辱骂仇恨词库、广告/诈骗/自伤风险词命中即打标 |
| 模型增强（可选 P1） | 第三方内容安全 API 或自建分类 | 命中高风险词的内容送审或调用云内容安全接口提高准确率 |
| 人工审核 | 管理后台审核队列 | 高风险内容进队列，审核员放行/遮挡/退回/封禁 |
| 用户举报 | 举报表 + 优先级队列 | 用户举报进入同一处置后台 |

敏感信息（联系方式）应在**用户提交前前端提示 + 后端强制遮挡**双保险。

### 7. 部署：容器化常驻，慎用纯 Serverless

本产品有**定时任务（超时释放）、队列消费者、长连接通知**等常驻需求，纯 Serverless 早期反而增加复杂度和冷启动问题。推荐：

- Docker 容器化（Next.js、NestJS、Worker 三类进程或合并）
- 云服务器 + 反向代理（Nginx/Caddy）+ CDN
- 托管的 PostgreSQL、Redis（省运维）
- 面向中国大陆运营须完成备案、隐私、内容治理合规（计划书十三章已强调，需专业人员复核）

---

## 四、各环境与服务清单（对应计划书十三章「部署基础组件」）

| 组件 | MVP 方案 | 说明 |
|---|---|---|
| Web 服务器 | 容器 + Nginx/Caddy | 静态资源 + 反代到应用 |
| 应用服务 | Next.js（前端 SSR）+ NestJS（API）+ Worker | 可同机不同进程 |
| 数据库 | 托管 PostgreSQL，每日备份 | 主备或快照备份 |
| 缓存 | 托管 Redis | 限流/锁/队列/验证码 |
| 对象存储 | 云 OSS / S3 | 信纸素材、数据导出 |
| CDN | 云 CDN | 静态资源加速 |
| 日志 | 云日志服务 | 应用日志、审计日志 |
| 异常监控 | Sentry | 前后端异常报警 |
| 备份 | 数据库每日快照 + 定期恢复演练 | 计划书要求可恢复 |
| 通知 | 云邮件 + 云短信 | 验证码、回信通知 |

---

## 五、不选用 / 暂缓的技术（避免过度设计）

| 技术 | 结论 | 原因 |
|---|---|---|
| 微服务架构 | MVP 不用 | 单体足够，过早拆分增加运维成本 |
| GraphQL | 不用 | REST 已满足，接口简单 |
| Kafka / RabbitMQ | 暂缓 | BullMQ 够用，规模上来再评估 |
| 即时通讯（WebSocket/IM） | 不用 | 计划书明确不开放即时私聊；通知用轮询/邮件即可 |
| 复杂富文本 / 图片视频 | 不用 | 违背书信纯文本定位 |
| 移动端原生 App | 不做 | 计划书明确只做移动优先 Web |
| 推荐算法 / 大数据平台 | P2 再说 | 早期信件分配用规则即可 |
| Kubernetes | 暂缓 | 单机或少量容器，Docker Compose 足够 |

---

## 六、技术选型与功能优先级（P0/P1/P2）的对应

- **P0（必须上线）**：上表全部核心栈即可覆盖——注册登录、写信投递、领取、回信、信箱、通知、举报、审核、后台、风控、统计。
- **P1（上线后优先）**：双方继续通信（在 `conversations` 上扩展，仍走信件状态机，不引入 IM）、信纸样式（对象存储 + 配置）、信誉等级（基于 `trust_score` 计算）、邮件提醒（已有邮件服务）。
- **P2（规模稳定后）**：未来信（定时投递→BullMQ 延迟任务）、声音来信（对象存储 + 转码服务）、多语言（i18n）、推荐分配（届时再引入算法/模型）。

选型的关键价值在于：**P0 选定的栈不需要为 P1/P2 推倒重来**，只做加法。

---

## 七、风险与对策（技术视角）

| 风险 | 对策 |
|---|---|
| 一封信被重复领取 | PG 行锁 `FOR UPDATE SKIP LOCKED` + 唯一约束 + Redis 锁双保险 |
| 领取超时未释放 | BullMQ 延迟任务 + 兜底定时扫描，幂等释放 |
| 写信内容丢失 | 前端本地草稿（localStorage）+ 后端草稿接口，自动保存防抖 |
| 敏感信息泄露 | 提交前前端检测 + 后端强制遮挡 + 审核兜底 |
| 恶意重复注册 | 手机号/邮箱唯一 + IP/设备风控 + 限流 |
| 审核成为瓶颈 | 自动检测先行过滤，人工只处理高风险队列；审核全程留痕可追溯 |
| 外包交付不完整 | 代码库、UI 源文件、数据库结构、部署文档、管理后台均纳入合同（计划书十五章） |

---

## 八、结论

MVP 推荐技术栈一句话总结：

> **TypeScript 全栈（Next.js 前端 + NestJS 后端）+ PostgreSQL + Redis/BullMQ + Ant Design 后台 + 容器化部署 + Sentry 监控。**

它满足计划书的全部要求，足够小团队驾驭，把复杂度集中在真正重要的地方——**信件状态机的可靠性**与**内容安全管线**，而不是炫技。详细模块边界与数据流见配套的《架构设计文档》。
