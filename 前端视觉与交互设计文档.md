# 匿名书信治愈社区 · 前端视觉与交互设计文档

> 适用范围：「标准 MVP」路线，移动端优先 Web。本文档补齐工程文档缺失的**视觉、情绪、拟物、动效、写信欲**这一层，供美工（UI/动效设计师）与前端共同执行。
> 配套：《架构设计文档》《模块拆分文档》《数据模型文档》。
> 设计目标一句话：**让人一进来就想安静地写一封信，写起来像在真实纸上书写，读起来像在拆一封陌生人寄来的温柔的信。**

---

## 〇、本文档锁定的三个产品主决策

| 决策 | 选择 | 落地要点 |
|---|---|---|
| 手写字体 | **正文也用手写体**（用户拍板，覆盖计划书"正文勿全用手写"的默认建议） | 必须用**高可读性**手写字体 + 宽行距 + 限制行宽 + **一键切回印刷体**的无障碍开关兜底（见 §3） |
| 拟物动画 | **克制精致** | 趴桌写信小动画放首页/空状态营造氛围；投递/拆信 1~2s 轻动效；正文不做真实翻页，改**平滑滚动 + 轻微页面转场**（见 §5） |
| 整体调性 | 温馨、安静、克制、有仪式感 | 纸张色系 + 暖黄灯光 + 大量留白 + 慢动效，不出现热榜/红点轰炸（见 §1、§2） |

> 关于正文手写体的提醒：手写体长文阅读疲劳是客观风险。本设计的对策是——**选一款笔画清晰、字重适中的中文手写体**（非花体草书），正文行距拉到 1.9~2.0，每行 ≤ 24 全角字，并在阅读页常驻"Aa 切换印刷体"按钮。把"沉浸"做成默认，把"可读"做成随手可得的退路。

---

## 一、设计语言（Design Language）

### 1.1 关键词
**安静 · 真诚 · 克制 · 温暖 · 等待 · 陪伴。** 任何设计元素若让人感到"吵、快、被催促、被比较"，即不符合本产品。

### 1.2 设计原则（可作为评审 checklist）
1. **留白即温柔**：信息密度低，单屏聚焦一件事。
2. **慢，但不卡**：动效缓慢柔和（缓动曲线偏 ease-out），但绝不阻塞操作，可被跳过。
3. **纸感而非塑料感**：纹理、阴影柔和，避免高饱和、强投影、玻璃拟态。
4. **文字是主角**：装饰服务于文字，不喧宾夺主。
5. **没有社交焦虑信号**：无点赞数、无粉丝、无热榜、无"正在输入"、无红点轰炸（仅"信箱有新信"一个柔和提示）。
6. **移动端第一**：所有设计先在 375px 宽度成立，再向上适配。

---

## 二、视觉规范

### 2.1 色彩系统（纸张 + 暖光）

| 角色 | 名称 | 取值（建议） | 用途 |
|---|---|---|---|
| 主背景 | 米白纸色 paper | `#F7F3EA` | 页面底色 |
| 次背景 | 浅米灰 paper-2 | `#EFE9DC` | 卡片/分区 |
| 信纸面 | 信纸白 letter | `#FBF8F1` | 信纸区域，略带暖调 |
| 正文文字 | 墨褐 ink | `#3A3026` | 正文、标题（非纯黑，柔和） |
| 次要文字 | 淡墨 ink-2 | `#8A7E6C` | 时间、提示、占位 |
| 强调/邮戳 | 暗红 stamp | `#A8453A` | 邮戳、关键按钮、少量强调（克制使用） |
| 暖光点缀 | 灯黄 glow | `#E8B86D` | 灯光氛围、悬停微光 |
| 分隔/描边 | 浅褐线 line | `#D9CFBC` | 分隔线、边框 |
| 成功/平和 | 苔绿 calm | `#7C8B6B` | 极少量状态色 |

夜间模式：背景转 `#23201B`，信纸转 `#2B2720`，文字转 `#E6DECF`，暖光降低——**夜间阅读不刺眼**（计划书移动端要求）。

### 2.2 字体系统

| 用途 | 字体策略 | 说明 |
|---|---|---|
| **正文（默认手写）** | 中文：可读性高的手写体（如 *悠哉字体 / 全字库说文 / 站酷快乐体* 类**清晰**手写体，避免花体草书）；提供 `font-display: swap` 与系统字体回退 | 行距 1.9~2.0，字号 ≥ 17px，每行 ≤ 24 全角字 |
| **正文（印刷体退路）** | 思源宋体 / 系统衬线，宋体最贴书信感 | 一键切换，记住用户偏好 |
| 标题 / 签名 / 日期章 | 手写体（可更花一点） | 仅短文本，沉浸感主要来源 |
| UI 文案（按钮/导航/表单） | 思源黑体 / 系统无衬线 | **始终印刷体**，保证可用性 |
| 数字 / 信件编号 | 等宽或衬线小字 | 邮戳、编号 |

**字体加载策略（性能关键）**：手写体中文字库大，必须
- 子集化（按常用字 + 动态按需子集，如 `fontmin` / CDN 动态子集服务）；
- `font-display: swap`，先用系统字体顶上，避免白屏；
- 写信编辑区考虑用印刷体输入、提交后/预览时再渲染手写体（减轻输入卡顿）——见 §4。

### 2.3 间距、圆角、阴影
- 间距基准 8px 栅格；页面左右留白手机端 ≥ 20px。
- 圆角柔和：卡片 12~16px，按钮 10px。
- 阴影柔和低对比：`0 4px 16px rgba(58,48,38,.08)`，避免硬投影。
- 纸张纹理：极轻的 noise/纸纹叠加（opacity ≤ 6%），不可影响文字对比度。

### 2.4 拟物元素清单（适度，计划书要求）
信封、邮戳、邮箱、信纸、拆信动作、投递动作、日期印章、信件编号。**用来点缀氛围，不做成寄信模拟游戏**。

---

## 三、正文手写体的可读性保障（重点，因正文用手写）

由于正文采用手写体，以下为**强制规范**，缺一项都会显著伤害长信阅读：

1. **字体选型**：笔画清晰、字重均匀、字间距规整的手写体；**禁用**连笔草书、过细、过艺术化字体。美工需用一段 300 字示范信在 375px 手机上做真机可读性验证后定稿。
2. **排版**：行距 1.9~2.0；段间距 ≥ 1 行；每行全角字 ≤ 24；首行不缩进或缩进 2 字统一。
3. **对比度**：墨褐 `#3A3026` over 信纸 `#FBF8F1`，对比度需达 WCAG AA（正文 ≥ 4.5:1）。
4. **退路常驻**：阅读页右上角常驻 `Aa` 按钮——切换"手写 / 印刷"、调字号（小/中/大）；偏好持久化（localStorage + 用户设置）。
5. **降级**：手写字体加载失败或超 1.5s 未就绪，自动回退印刷体，不白屏。
6. **无障碍**：尊重系统"减少动态效果"(`prefers-reduced-motion`)；屏幕阅读器读取的是文本而非字体外观，正常。

> 这条 §3 是对你"正文手写体"决定的工程兜底。建议保留，否则长信流失风险高。

---

## 四、核心页面视觉与交互设计

### 4.1 首页（激发写信欲——转化的关键）
目标：3 秒内让人"懂这是什么"并"想写一封信"。

```
┌─────────────────────────────┐
│        （暖黄灯光氛围）          │
│   〔趴桌写信的小动画——一个人      │
│    伏案，灯下，笔尖偶尔轻动〕      │   ← 克制精致：循环极慢、可被滚动带走
│                               │
│   有些话，不知道该说给谁听。       │   ← 手写体大标题（情绪钩子）
│   把它写成一封信，               │
│   寄给一个愿意认真读完的人。       │
│                               │
│   ┌─────────┐  ┌─────────┐    │
│   │ ✎ 写一封信 │  │ ✉ 读一封信 │   │   ← 两个主按钮，写信为主(暗红强调)
│   └─────────┘  └─────────┘    │
│                               │
│   · 这里是什么 · 可以做什么       │   ← 极简说明，向下滚动渐次出现
│   · 少量匿名用户故事（信纸卡片）    │
│   · 社区原则一句话 + 登录入口      │
└─────────────────────────────┘
```
要点：
- **首屏只放情绪钩子 + 两个按钮**，不堆功能。
- 趴桌写信动画是氛围担当（§5.1），但**不阻塞、可滚动跳过、尊重 reduce-motion**。
- 用户故事用"信纸卡片"形式，弱化点赞/数字。

### 4.2 写信页（降低门槛——让人写得下去）
目标：安静、不被表单打断，**先写，后填**。

```
┌─────────────────────────────┐
│  ←            写一封信         │   ← 顶栏极简
│ ┌─────────────────────────┐ │
│ │（信纸纹理 + 暖光，可选信纸样式）│ │
│ │                          │ │
│ │  此刻，你想说点什么……         │ │ ← 占位引导语（柔和）
│ │  ▏                        │ │ ← 大面积输入区，光标即写
│ │                          │ │
│ │                          │ │
│ └─────────────────────────┘ │
│  〔可选〕主题 ▾  心情 ▾         │ ← 折叠在下方，非必填先不打扰
│  字数 128 · 已自动保存 ✓        │ ← 轻提示，不催促
│            ┌──────────┐       │
│            │  封好信件  │      │ ← 主按钮
│            └──────────┘       │
└─────────────────────────────┘
```
要点（直接对应计划书"风险六：界面好看但用户不写信"的应对）：
- **进来就能写**：光标默认聚焦正文，主题/心情/回信偏好**默认折叠**，写完再选。
- **写作引导问题**：占位语或"不知道写什么？"轻提示给 2~3 个引导问句（如"最近最让你放不下的一件事是？"）。
- **草稿自动保存**：防抖本地保存 + 服务端兜底；显示"已自动保存"。
- **最低字数**：柔性提示（如建议 ≥ 50 字），不强行卡死。
- **输入性能**：编辑区**输入态用印刷体或轻量手写体**，避免大字库手写体边打边渲染卡顿；点"预览/封信"时再呈现手写信纸效果。
- **敏感信息前端预检**：检测到手机号/微信号等，柔和高亮提示"为保护你和对方，建议不要留下联系方式"。
- **防丢失**：返回/切后台不丢内容；键盘弹起编辑区不被遮挡。

### 4.3 投递动画页（仪式感，1~2s）
点击"封好信件" → 信纸折叠进信封 → 信封封口 → 邮戳盖下（暗红）→ 投入邮箱。1.5s 左右，**可点击跳过**。完成后给一句温柔反馈："你的信已经寄出，正在等待一个愿意认真读它的人。"

### 4.4 待回信箱 / 读信页（每次一封）
- **每次只出现一封信**（计划书要求），不做信息流列表。
- 卡片正面：信封 + 主题 + 模糊时间（"两天前"）+ 一句提示，**领取**按钮 + 举报。
- 点"领取这封信" → 信封展开为信纸（§5.3 拆信动效）→ 进入阅读。
- 阅读页：手写体正文（带 §3 可读性保障 + `Aa` 切换）、模糊时间、主题、领取/回信入口、举报。隐藏精确时间与作者资料（防身份推测）。

### 4.5 回信页
- 上方展示原信（可折叠），下方回信输入区。
- 边缘常驻**温和引导**："先回应对方的感受，再表达自己的看法。""不必解决所有问题，认真读完本身就是一种回应。"
- 草稿自动保存；提交后回到"等待审核/已寄出"的温柔反馈。

### 4.6 我的信箱 / 拆阅回信
- 分区：寄出 / 收到 / 回复中 / 收藏 / 草稿。状态用柔和文案（已投递、等待领取、已被领取、正在写回信、已收到回信、已结束）。
- **收到回信**：不在通知里剧透内容，提示"你的信箱里有一封新来信"。进入后是**未拆封的信封**，点击 → 拆信动效 → 展开正文（§5.3）。这是产品仪式感的高潮，重点打磨。

---

## 五、动效规范（克制精致）

总原则：缓慢、柔和、有意义、可跳过、尊重 `prefers-reduced-motion`（减弱或关闭）。技术上优先 CSS transition / transform 与 Framer Motion；避免 JS 长动画阻塞主线程；移动端控制重绘。

### 5.1 趴桌写信动画（首页/空状态氛围）
- 形式：**2D 插画 + 极轻微循环**（笔尖轻动、灯光微微呼吸、偶尔眨眼），非 3D、非常驻高帧动画。
- 实现：Lottie（矢量、体积小、可控）或少量帧 SVG 动画；循环周期慢（≥ 6s），CPU 占用低。
- 约束：滚动离开即停；reduce-motion 下显示静态插画；不做声音。

### 5.2 投递动画
信纸折叠 → 封口 → 邮戳落下 → 入箱。总时长 1.2~1.8s，缓动 ease-out，**可跳过**。

### 5.3 拆信 / 信件展开（替代"翻页"）
- 你提到"翻页动画"——书信产品更贴切的是**拆信 + 信纸展开**而非书本翻页：
  - 信封盖掀起（0.4s）→ 信纸抽出/展开（0.6s）→ 正文淡入（0.3s）。
- **正文长内容不做真实翻页**：长信用**平滑垂直滚动**阅读，配合页面进入/退出的**轻微转场**（淡入 + 轻微位移 8~12px），既有翻阅感又不伤长文阅读与性能。
- 如确需"页"的感觉，仅在**短信件/卡片切换**时用轻量横向滑动转场（≤ 0.4s），不在长正文里用 3D 翻页。

### 5.4 微交互
- 按钮悬停：暖光微亮（glow），轻微上浮 1~2px。
- 状态切换、卡片出现：淡入 + 轻位移。
- "已自动保存"：极轻的对勾淡入淡出，不抢注意力。
- 全站**禁用**：强烈弹跳、闪烁、红点徽标轰炸、强制全屏遮罩广告式动画。

### 5.5 动效性能与降级
- 移动端动画用 `transform`/`opacity`，避免触发 layout。
- 首页插画动画懒加载，首屏可交互不被动画拖慢。
- `prefers-reduced-motion: reduce` → 所有装饰动画降级为即时或淡入。
- 低端机/弱网：动画可整体关闭（设置项），功能不受影响。

---

## 六、组件库与美工交付清单

### 6.1 拟物 / 业务组件（前端实现 + 美工提供素材）
| 组件 | 说明 | 素材需求 |
|---|---|---|
| `LetterPaper` 信纸 | 可换样式，纹理 + 暖光 | 信纸纹理图/样式若干（P1 可扩展） |
| `Envelope` 信封 | 正面/拆封态 | 信封图、封口、内衬 |
| `Stamp` 邮戳 | 落章动效，暗红 | 邮戳 SVG（含日期章变体） |
| `Mailbox` 邮箱 | 投递目标 | 邮箱插画 |
| `DeskWriter` 趴桌写信 | 首页氛围动画 | **Lottie/SVG 动画源文件** |
| `LetterCard` 信件卡片 | 待回信箱/用户故事 | —— |
| `MoodPicker`/`TopicPicker` | 心情/主题选择，折叠式 | 心情/主题小图标 |
| `Aa` 阅读设置 | 字体切换/字号 | —— |

### 6.2 基础 UI 组件
按钮、输入区（信纸态）、折叠面板、Toast（柔和）、空状态、加载（暖光呼吸）、底部导航（移动端）、对话框（举报/确认继续通信）。

### 6.3 美工必须交付（写进外包合同——计划书十五章）
- 品牌视觉（logo、字体方案、配色规范文件）。
- 全部页面移动端 + 桌面端高保真稿（Figma 源文件可交付）。
- **趴桌写信动画 + 投递 + 拆信动效的 Lottie/源文件**。
- 信纸 / 信封 / 邮戳 / 图标素材（含夜间模式）。
- 动效规范（时长、缓动曲线、reduce-motion 行为）。

---

## 七、文案与情绪设计（写信欲的另一半）

视觉之外，**文案**决定用户愿不愿意写。统一温柔、第二人称、不说教、不催促。

| 场景 | 推荐文案方向 |
|---|---|
| 首页钩子 | "有些话，不知道该说给谁听。把它写成一封信，寄给一个愿意认真读完的人。" |
| 写信占位 | "此刻，你想说点什么……" / "不知道从哪写起？就从最近放不下的那件事开始。" |
| 投递成功 | "你的信已经寄出，正在等待一个愿意认真读它的人。" |
| 收到回信 | "你的信箱里有一封新来信。" |
| 回信引导 | "先回应对方的感受，再表达自己的看法。不必解决所有问题。" |
| 空信箱 | "还没有新的来信。好的回信，值得等待。" |
| 紧急风险提示 | 温和但清晰地给出现实求助渠道（非诊断、非劝说式） |

文案集中在 `content_configs` 管理（见数据模型），便于运营调整。

---

## 八、可访问性与移动端兜底（不可省）

- 正文手写体 + `Aa` 印刷体退路（§3）。
- 全站对比度达 WCAG AA。
- `prefers-reduced-motion` 全面支持。
- 键盘可达、表单 label 完整、焦点可见。
- 触控目标 ≥ 44×44px；长信写作防误触。
- 弱网/低端机：字体与动画可降级，核心功能不依赖动效。

---

## 九、与已有文档的衔接

- 本文档的页面与《模块拆分文档》§6.1 `apps/web` 路由一一对应；新增 `DeskWriter` 等拟物组件归入 `components/`。
- 字体偏好、阅读设置持久化到用户设置（《数据模型文档》users 表可加 `prefs` 或走前端 localStorage + 设置接口）。
- 文案走 `content_configs`，运营可改。
- 动效与字体的**性能预算**应纳入第 9 周"移动端测试/压力测试"验收。

---

## 十、结论与一句话提醒

本文档把"温馨、写信真实感、克制精致动画、简洁好上手、激发写信欲"落到了可执行的视觉/动效/文案规范，并对你选择的**正文手写体**配了可读性兜底（§3）。

> 最后一句提醒：这个产品打动人的，**先是"我真的想写、写得下去、收到了认真的回信"，其次才是信封动画和手写字体**。视觉和动效要服务于这份安静与真诚，而不是盖过它。建议上线前用真机和真实长信，验证"手写正文 + 动效"在手机上的可读性与流畅度，再定最终方案。
