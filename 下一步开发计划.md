# 回声邮局 · 本轮开发计划

## 主题：登录门禁 · 极简化 · 真实书信美术

> 本轮不碰后端治理（RBAC / 审核 / 举报 / 会话安全等已完成），只打磨**用户第一眼与写信读信的体验**。
> 一句话目标：**未登录的人只看到一个安静的门厅；登录后的人，感觉自己在写一封真正的信，而不是填一张表单。**

---

## 〇、本轮聚焦（来自用户反馈原话）

1. **逻辑**：「我还没有登录，不应该直接能看到写信读信等选项按钮。」
2. **极简**：「美术上没有做到极简，有很多意义不大的字段。」
3. **真实感**：「美术也可以在多方面贴近真实写信。」

---

## 一、问题定位（落到文件）

| 反馈 | 现状 | 位置 |
| --- | --- | --- |
| 未登录就暴露入口 | 首页对所有人渲染「写一封信 / 读一封信」大按钮 + 底部导航（写信/读信/信箱/我的），点了才跳登录 | `apps/web/src/app/page.tsx`、`components/BottomNav.tsx` |
| 字段冗余 | 12 个主题 + 8 个心情 + 标题 + 「主题·心情 ▾」折叠区 | `lib/labels.ts`、`app/write/page.tsx:161-192` |
| 读信端也堆字段 | 顶部一排「主题 / 心情 / 时间」药丸 | `app/read/page.tsx:97-101` |
| 不够像写信 | 有信纸手写体 + 拆信动效，但缺信封 / 邮票 / 邮戳 / 折信入封 / 抬头落款的实体仪式感 | `components/Envelope.tsx`、`write/sent` 投递页 |
| 真实感被外网拖累 | 手写体走 Google Fonts，离线/慢网回退系统字体，首屏"写信感"打折 | `app/layout.tsx:19-27` |

---

## 二、登录门禁重构（逻辑）

**原则：未登录 = 门厅，只讲这里是什么 + 一个「进来」入口；功能入口与底部导航一律不出现。**

### 2.1 路由分层
- [ ] 用 App Router **路由组**分离：
  - `(public)`：门厅 `/`、`/auth`、`/rules`（社区约定可公开）。
  - `(app)`：`/write`、`/read`、`/mailbox/**`、`/reply/**`、`/settings`、`/admin/**` —— 受保护。
- [ ] `(app)/layout.tsx`：统一做登录校验（`useProfile`，未登录 → `/auth?next=`），并**只在这里**渲染 `BottomNav`。门厅与登录页不挂底部导航。
- [ ] 保留 `useRequireAuth` 作为逐页兜底，但视觉上不再"先闪按钮再跳走"。

### 2.2 门厅页（未登录 `/`）
- [ ] 只有：`DeskWriter` 氛围动画 + 一句情绪钩子 + **一个** CTA「进来，写一封信」（→ `/auth`）+ 一行「已有账号？登录」。
- [ ] 去掉写信/读信双大按钮、底部导航、匿名故事墙（故事墙移到登录后的门厅，或保留但不带任何功能入口）。
- [ ] 登录态访问 `/`：显示"书桌"——写信 / 读信 / 信箱入口（即现有 hub，迁到 `(app)`）。

### 2.3 验收
- 退出登录后打开站点：看不到任何写/读/信箱/我的入口，也没有底部导航，只有门厅 + 登录入口。
- 登录后：底部导航出现，`/` 变为可写可读的书桌。
- 直接敲 `/write` 且未登录：跳 `/auth?next=/write`，登录后自动回到写信页。

---

## 三、极简化：砍掉无意义字段

**原则：写信页面上，除了信纸和一个「封好信件」，能不出现的都不出现。**

### 3.1 写信页
- [ ] **移除心情（mood）选择**：8 个心情标签整段删除（enum 保留以兼容后端，UI 不暴露）。
- [ ] **弱化/隐藏标题**：真实的信很少有标题。默认不显示标题输入框；正文即全部。（读信端用正文首句作展示。）
- [ ] **收敛主题（category）** —— 见 3.3 决策。
- [ ] 删除「主题·心情 ▾」折叠区，页面只剩：信纸 + 字数提示 + 底部「封好信件」。
- [ ] 保留：≥50 字下限、联系方式预检、危机词温和提示、Tab 段首空两格、自动存草稿。

### 3.2 读信页
- [ ] 去掉顶部「主题 / 心情」药丸，最多保留一处极轻的模糊时间（「几天前的一封信」）。
- [ ] 页面聚焦信纸本身；领取 / 换一封 / 举报 保留但更收敛。

### 3.3 主题（category）处理 —— 已定：方案 A

> 用户已拍板 **A**：**前端零字段，后端隐性推断**。
> 写信不再选主题；`category` 在创建时默认 `JUST_TALK`（或后端按正文轻量关键词归类）。分配算法退化为时间序 + 少量随机（把 P2 的兴趣加权降级为可选、不依赖用户手选主题）。日后若留存数据证明兴趣匹配价值大，再回到「6 类可跳过」方案。

- [ ] 写信页移除主题选择 UI。
- [ ] `claim.peek`：`interestedTopics` 为空时走纯时间序兜底（已具备），确认无主题也能正常分配。

### 3.4 验收
- 写信页从上到下：抬头 → 信纸 → 「封好信件」。无心情、无标题框、无折叠区、无主题选择。
- 读信页无一排标签药丸。
- 后端不因缺字段报错（title/mood/category 均可空，DTO 已 optional，复核一遍）。

---

## 四、更贴近真实写信的美术

**原则：把"写信—封信—寄出—收信—拆信"做成一套连贯的实体仪式，减少 App 味（按钮、药丸、卡片），增加纸与信封味。**

> **本轮基调（用户明确）：在保持极简的前提下，用美术把用户"吸"进真实写信的感觉。** 极简是减字段、减 chrome；美术是加质感、加仪式、加沉浸。两者不矛盾——每一处新增动效/纹理都要为"这是一封真的信"服务，任何不服务于此的装饰都不加。

### 4.1 信纸与书写（写信页）
- [ ] 信纸顶部固定抬头「致 一位素未谋面的你」，底部落款区「—— 一个给你写信的人」。
- [ ] 纸张质感：淡淡泛黄 + 极轻纸纹 + 内阴影（像一张摊在桌上的纸），横线已对齐（保留）。
- [ ] 书写反馈：光标/落笔更像墨水（可选，克制）。

### 4.2 封信 → 寄出（`write/sent` 投递动画增强）
- [ ] 点「封好信件」：信纸**折叠**→ 装入**信封** → 盖**邮戳** → 信封滑出寄走的连贯动效（framer-motion，时长克制 ~1.2s）。
- [ ] 落地文案「你的信已经寄出，正在寻找一个愿意认真读完的人」。

### 4.3 收信 → 拆信（信箱/拆信页）
- [ ] 信箱里未读信呈现为**躺在桌上的信封**（含邮票 / 邮戳 / 模糊日期），而非列表卡片。
- [ ] 拆信动效复用/升级现有 `Envelope`：封口撕开 → 抽出信纸 → 展开。

### 4.4 手写体自托管（真实感的地基）
- [ ] 把 Ma Shan Zheng **子集化**（常用汉字子集）放 `apps/web/public/fonts`，`@font-face` 本地加载，去掉 `layout.tsx` 里的 Google Fonts 外链。
- [ ] `font-display: swap`，系统字体兜底；首屏不再等外网。
- **验收**：断网状态下写信页正文仍是手写体；首屏无对 `fonts.googleapis.com` 的请求。

### 4.5 减 App 味
- [ ] 复核全站：把"药丸标签 / 卡片 / 明显按钮"在书信场景里尽量替换为更纸感的处理；保留必要操作按钮但弱化描边。
- **验收**：写信、读信、拆信三屏截图，主观上"像信"而非"像表单/信息流"。

---

## 五、执行顺序（纵向切片，每片 tsc + build 全绿后提交）

1. **切片一 · 登录门禁**（第二章）：路由分层 + 门厅页 + 底部导航仅登录可见。`refactor(gate):`
2. **切片二 · 极简字段**（第三章，按决策 A）：删心情、隐标题、去主题、清读信药丸。`refactor(minimal):`
3. **切片三 · 书信美术**（第四章 4.1–4.3）：抬头落款 + 折信入封寄出 + 信封化信箱。`feat(craft):`
4. **切片四 · 字体自托管 + 减 App 味**（4.4–4.5）。`feat(craft):`

> 建议一片一提交、一片一自查截图，避免一次改动过大难回退。

---

## 六、明确不在本轮范围

- 后端治理 / 审核 / 举报 / 会话安全（已完成，不动）。
- 继续通信 Conversation、数据导出、自动化测试、Sentry、`next` 补丁升级 —— 顺延至体验轮之后单独排期。

---

## 七、进度追踪（随开发更新）

> 分支 `feat/foyer-craft`（基于 feat/governance-admin）。每片 `tsc --noEmit` + `next build` 全绿后提交。

- [x] 切片一 登录门禁 — `7d82665`
  - 首页按登录态分叉：未登录=门厅（氛围动画+钩子+单一「进来写信」入口，无功能入口、无底部导航），登录=书桌。
  - `BottomNav` 未登录返回 null；`useUnread(enabled)` 未登录不轮询。
- [x] 切片二 极简字段（决策 A） — `8a98bb5`
  - 写信页删标题/心情/主题/折叠区，只剩 信纸→字数→封好信件；读信页去药丸。
  - `submitLetterSchema.category` 改 `default(JUST_TALK)`：前端零字段、后端兜底。
- [x] 切片三 书信美术 — `1851e2d`
  - `.paper-grain` 泛黄纸纹+内阴影；写信抬头/落款；读信/回信称呼顶格。
  - 寄信页重做为 折叠→入封→封盖→盖邮戳→寄出 连贯仪式；信箱收到信=躺桌上未拆的信封。
- [x] 切片四 字体自托管 + 减 App 味 — `e192728`
  - Ma Shan Zheng → woff2（3.3MB 全覆盖）自托管 `public/fonts`，移除 Google Fonts 外链；印刷体改系统宋体族。
  - 回信页统一纸感（纸纹+抬头+落款）。

### 本轮完成小结

四片全部落地，`next build` 全绿。核心变化：**未登录只见门厅**（功能入口/导航全部隐藏）、**写信读信极简到只剩信纸**（决策 A：主题后端兜底）、**书信仪式感**（纸纹/抬头落款/折信寄出/信封化信箱）、**字体离线自托管**（去 Google Fonts 依赖）。

需你本地验证：`npm run dev:web` 后依次看①退出登录访问 `/`（应只有门厅）②登录后写信页（只有信纸+抬头落款）③封好信件的寄出动画④信箱里的信封样式⑤断网仍是手写体。后端若跑，注意 `packages/shared` 需 `npm run build`（category 默认值改动）。
