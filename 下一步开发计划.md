# 回声邮局 · 下一步开发计划

> 基于当前代码实际状态（不是文档理想态）盘点缺口，给出分优先级的完善路线。
> 现状：P0 后端核心闭环 + 前端书信体验已跑通并验证。以下是"接下来该做什么、按什么顺序"。

---

## 〇、现状盘点

**已完成**
- 后端 11 模块：auth / user / letter / claim / reply / mailbox / moderation / notification / report / content / worker，核心闭环（写信→审核遮挡→领取抢占→回信→通知）已在真实 PG+Redis 验证。
- 前端 10 页：首页 / 登录 / 写信 / 投递动画 / 读信领取 / 回信 / 信箱 / 拆信 / 设置 / 规则，信纸手写体 + 拆信动效已落地。

**关键缺口（决定下一步优先级）**
1. **治理侧是空的**：高风险内容会被标记进 `UNDER_MODERATION`，但**没有任何人能审核它**；举报能提交但**无处处理**；`audit_logs` 表建了但没写入。→ 这是最大的功能性空洞。
2. **管理后台整个没做**（`apps/admin` 不存在）。
3. **紧急风险处置缺失**：自伤词能识别，但提交时**不给用户紧急求助提示**，也没有单独处置队列。
4. **看板/统计没接**：`daily_stats` 表在，worker 没有聚合任务。
5. **会话安全偏弱**：logout 是空操作、refresh 无轮换/黑名单、限流按 `req.ip`（生产在代理后会串号）。
6. **信任/等级不生效**：`trustScore` 会随反馈加减，但 `level` 从不重算、配额从不随信誉提升。
7. **通知未接到 UI**：接口有，但首页/底栏不显示未读、信箱无"新信"红点。

---

## 一、第一优先：补齐安全与治理闭环（最该先做）

> 计划书原话：安全系统应与写信功能**同时**建设。现在写信侧好了，治理侧必须马上补，否则一旦有真实用户，高风险内容和举报会直接失控。

### 1.1 管理后台（新增 `apps/admin` 或先在 `apps/web/admin` 路由）
- 数据看板：新增/活跃用户、新增/已回复/未回复信件、平均等待、举报数、高风险数、回信完成率、北极星（每周有效往来）。
- 信件/回信审核队列：看正文 + 风险标签 + 命中明细（`sensitive_hits`）+ 遮挡放行 / 退回 / 拒绝。
- 举报处理：分类、优先级、处理结果、复核，写回 `reports.status`。
- 用户管理：行为/举报记录、限制写信/回信、临时/永久封禁、申诉。
- 内容配置：改首页文案 / 社区规则 / 风险提示（`content_configs`，已有读接口，补写接口）。

### 1.2 后端 admin 模块（`apps/api/src/modules/admin`）
- RBAC：`@Roles('admin'|'moderator')` 守卫已在，缺**角色落库 + 授予入口**（现在没人能变成 admin）。→ 加 `users.role` 字段 + 一个 seed/CLI 把某账号设为 admin。
- 审核动作复用 `ModerationService.review()`，并**推进状态机**：放行→`PUBLISHED`/`WAITING_CLAIM`，拒绝→`REJECTED` + 通知作者。
- 所有 admin 写操作经 `AuditInterceptor` 落 `audit_logs`（表已建，接一下）。

### 1.3 紧急风险处置（自伤/危机）
- 提交含自伤风险词时：**前端立即弹出温和的紧急求助提示**（现实求助渠道，非诊断），内容进**独立高优审核队列**。
- 后端：`RiskLevel.HIGH` 且命中 `SELF_HARM` → 打特殊标记，审核后台置顶。
- 不让普通回信者承担救助责任（计划书风险四）。

**产出**：一个能真正运营的治理后台 + 危机处置流程。没有这一层，产品不具备上线条件。

---

## 二、第二优先：逻辑健壮性优化

### 2.1 会话与安全
- **logout 真正失效**：refresh token 用 `jti` + Redis 黑名单；access 短时（已 15m）。
- **refresh 轮换**：每次刷新换新 refresh，旧的作废，防重放。
- **限流可信 IP**：Next 代理转发后端，后端应信任 `X-Forwarded-For`（`app.set('trust proxy')` + 取真实 IP），否则所有用户共享一个 IP 被一起限流。
- **CSRF**：cookie 目前 `sameSite=lax`，写操作建议加双提交 CSRF token。
- **注册限流按邮箱+IP 双维度**，防同 IP 批量注册。

### 2.2 信件分配算法（`claim.peek` 优化）
现在是"取最老的一封"，过于简单。按计划书应：
- 按读者 `interestedTopics` 加权优先，但**放宽兜底**避免空池饥饿。
- 风险分层：高风险信不进普通池（已做），加"新用户只分配低风险信"。
- **长期无人领取的信人工兜底**：超过 N 小时未被领取 → 进种子回信者队列 / 后台提醒。
- 领取上限、`max_replies`、跳过集合（已做）继续保留。

### 2.3 信任分与等级生效
- `level` 定时/触发重算（`trustScore` + 有效回信数 + 违规），映射 `NEWCOMER→KEEPER`。
- 配额随等级动态提升（现在恒定 1/2）：高信誉者更高领取额度（计划书十一章）。
- 低质量回信者（多次"不舒服"反馈）降额度。

### 2.4 数据一致性
- `claim.releaseAllExpired` 目前逐条串行释放，量大时改批处理 + 加锁避免与延迟任务重复释放（已幂等，但可减压）。
- 回信发布、claim 置 `REPLIED`、letter 状态推进三步应在**一个事务**内（现在分步，极端情况下可能不一致）。
- 多回信信件（`maxReplies>1`）达上限后自动 `CLOSED` 的边界补测试。

---

## 三、第三优先：功能加强（提升体验与留存）

### 3.1 通知接入 UI（留存关键）
- 底栏/首页显示**未读红点**；收到回信时"你的信箱里有一封新来信"实时提示（轮询 `/notifications`，MVP 不上 WebSocket）。
- 拆信后自动标记已读。

### 3.2 冷启动内容扩充
- `prisma/seed.ts` 增至 30~50 封**编码正确**的示范来信 + 50~100 封示范回信（计划书九章）。
- 首页展示少量**匿名用户故事**（现在没接）。
- 写作引导问句库、回信结构引导扩充。

### 3.3 继续通信（Conversation，P1）
- `conversations` 表已建，补模块：收到回信后可"申请继续通信"，双方同意才建有限往返信箱，**仍以一封信为单位**，不显示在线/正在输入。

### 3.4 数据权利与合规
- **数据导出**：设置页加"导出我的信件/回信"（异步生成文件存对象存储，限时下载）——注销已做，导出缺。
- 上线前：备案、隐私政策、用户协议、内容治理合规（请专业人员复核）。

### 3.5 前端体验细节
- **手写字体自托管**：把 Ma Shan Zheng 子集化放 `public/fonts`，摆脱 Google Fonts 外网依赖 + 首屏更快（现在依赖联网，离线回退系统字体）。
- 草稿列表可删除/继续；写信页返回不丢内容（已做）再加"离开确认"。
- 桌面端进一步适配（信纸更居中、更像一封信）。

---

## 四、第四优先：质量与可观测

- **自动化测试**：核心闭环 e2e（注册→写→领→回→通知）；**领取抢占并发测试**（多人抢同一封，断言只有一人成功）；moderation 遮挡/风险分级单测。
- **Sentry + 结构化日志**：前后端异常上报（设计文档已规划，未接）。
- **next 安全升级**：`next@14.2.15` 有安全告警 → 升到 14.2 最新补丁。
- **移动端真机验证**：手写正文长信的可读性与动效流畅度（计划书第 9 周验收项），据此定手写字体最终方案。

---

## 五、建议的执行顺序（未来 2~3 周）

1. **第 1 周**：后端 admin 模块 + RBAC 落地（角色字段 + 授予）+ audit_logs 接入 + 审核/举报处理接口。同时补紧急风险前端提示。
2. **第 2 周**：`apps/admin` 后台界面（看板 + 审核队列 + 举报 + 用户管理 + 内容配置）。
3. **第 3 周**：会话安全加固（logout/refresh/限流 IP）+ 分配算法优化 + 通知接入 UI + 冷启动内容扩充。
4. 之后：Conversation(P1)、数据导出、字体自托管、测试与监控。

> 一句话优先级：**先把"能审核、能处理举报、能处置危机"补上（治理闭环），再优化分配与信任逻辑，最后加继续通信等增值功能。** 因为对一个匿名书信社区，安全治理不是加分项，是上线的前提。

---

## 六、施工细则（可直接照做的文件级 TODO 与验收）

> 约定：每完成一项，在下方复选框打勾并注明 commit。所有改动保持 `tsc --noEmit` 与 `next build` 绿。

### P1-1 RBAC 落地
- [ ] `prisma/schema.prisma`：`User` 增 `role String @default("user")`（取值 `user|moderator|admin`，与 `AppRole` 一致）。
- [ ] 迁移：`npm run prisma:migrate`（需 Docker 起 PG）。
- [ ] `apps/api/src/modules/auth/auth.service.ts`：`login`/`refresh` 签发 token 时用**真实** `user.role`（当前硬编码 `'user'`）。
- [ ] 授予入口：`prisma/promote-admin.ts` 脚本，按邮箱把某账号设为 `admin`（`npx tsx prisma/promote-admin.ts <email>`）。
- **验收**：管理员账号登录后 `req.user.role==='admin'`，可过 `RolesGuard`；普通用户访问 `/admin/*` 返回 403。

### P1-2 审计日志
- [ ] `apps/api/src/common/interceptors/audit.interceptor.ts`：拦截带 `@Audit()` 标记的写操作，落 `audit_logs`（operator/action/target/before-after/ip）。
- [ ] `@Audit(action, targetType)` 装饰器。
- [ ] 挂到 admin 控制器的写方法。
- **验收**：每次后台放行/拒绝/封禁在 `audit_logs` 有一条不可删记录。

### P1-3 审核动作（服务层）
- [ ] `ModerationService.reviewLetter(operatorId, letterId, action, note?)`、`reviewReply(...)`：
  - `PASS`/`MASK_PASS` → 内容 `PUBLISHED`/信件 `WAITING_CLAIM`（首次）；
  - `REJECT` → `REJECTED` + 通知作者；
  - `RETURN` → 退回作者可改（`DRAFT`）；
  - `LIMIT`/`BAN` → 调 user 状态；
  - 全程 `moderation_records` 留痕。
- **验收**：一封 `UNDER_MODERATION` 的信可在后台放行后进入待领取池；拒绝后作者收到通知。

### P1-4 Admin 后端模块（`apps/api/src/modules/admin`）
- [ ] `dashboard`：GET `/admin/dashboard`（活跃/新增/待回/已回/举报/高风险/完成率）。
- [ ] 审核：GET `/admin/letters?status&risk`、GET `/admin/replies`、POST `/admin/letters/:id/review`、POST `/admin/replies/:id/review`。
- [ ] 举报：GET `/admin/reports?status&priority`、POST `/admin/reports/:id/handle`。
- [ ] 用户：GET `/admin/users/:id`、POST `/admin/users/:id/limit|ban|appeal`。
- [ ] 配置：GET/PUT `/admin/configs/:key`。
- 全部 `@Roles('admin','moderator')` + `@Audit`。
- **验收**：`curl` 用管理员 token 能拉审核队列、放行/拒绝、处理举报、封禁用户。

### P1-5 紧急风险处置
- [ ] 前端写信/回信提交前：命中自伤词 → 弹**温和紧急求助提示**（现实求助渠道，非诊断），可继续或取消。
- [ ] 后端：`SELF_HARM` 命中 → `risk=HIGH` 且进**高优审核队列**（`reports`/审核队列置顶标记）。
- **验收**：含自伤词的信提交时用户看到求助提示，且后台该信排在审核队列最前。

### P2 摘要（P1 完成后展开细则）
- 会话安全（logout 黑名单 / refresh 轮换 / 可信 IP 限流）
- 分配算法（主题加权 + 无人领取兜底）
- 信任分/等级重算 + 动态配额
- 回信发布三步合入单事务

---

## 七、自动推进进度（本文件由开发过程持续更新）

- 2026-07-06 起：按 P1 顺序执行。进度见各复选框与 `git log`。
- 每完成一个纵向切片提交一次，提交信息前缀 `feat(governance):` / `feat(admin):` 等。

### 已完成（切片一：治理闭环，typecheck + build 全绿）

- ✅ **P1-1 RBAC**：`users.role` 字段 + 迁移 `20260706020000_add_user_role` + `auth` 按真实角色签发令牌 + `prisma/promote-admin.ts` 授予脚本。
- ✅ **P1-2 审计**：`@Audit` 装饰器 + `AuditInterceptor`，挂在全部 admin 写接口，落 `audit_logs`。
- ✅ **P1-3 审核动作**：`AdminService.reviewLetter/reviewReply`（放行→发布/待领取、拒绝→通知作者、退回→草稿），全程 `moderation_records` 留痕。
- ✅ **P1-4 Admin 后端**：`/admin/dashboard`、`/admin/letters|replies`（+ 命中明细）、`/admin/*/review`、`/admin/reports` + `handle`、`/admin/users/:id` + `limit|ban|appeal`、`/admin/configs/:key`（GET/PUT）。全部 `@Roles('admin','moderator')`。
- ✅ **P1-4 Admin 前端**：`apps/web/src/app/admin`（dashboard / moderation 队列 / reports），`useRequireAdmin` 角色守卫。
- ✅ **P1-5 危机处置（前端）**：写信页命中自伤词 → 温和紧急求助弹窗（非阻断，可继续寄出）；后端高风险信按 `riskLevel desc` 在审核队列置顶。

### 待用户执行（需本地 Docker/DB）

1. 起 Docker → `npm run prisma:migrate`（应用 role 迁移）。
2. 注册一个账号 → `npx tsx prisma/promote-admin.ts <你的邮箱> admin` → 重新登录。
3. 访问 `http://localhost:3000/admin` 即可审核 / 处理举报。

### 已完成（切片二：P2 逻辑健壮性，typecheck + build 全绿）

- ✅ **P2.1 会话安全**：`main.ts` 开启 `trust proxy`（限流按真实客户端 IP）；refresh 令牌带 `jti`，刷新时**轮换**（作废旧 jti）+ 查 Redis 黑名单 + **重读用户角色/封禁状态**；`logout` 真正把 refresh 加入黑名单失效。
- ✅ **P2.2 分配算法**：`claim.peek` 按读者 `interestedTopics` 优先，`publishedAt ASC` 兜底（防非偏好主题饥饿）。
- ✅ **P2.3 信任分/等级**：`UserService.recomputeLevelAndQuota`，回信反馈后按 trustScore 重算等级并动态提升领取/投递配额。
- ✅ **P2.4 事务一致性**：回信发布（reply→PUBLISHED、claim→REPLIED、letter→REPLIED）合入单个 `$transaction`，通知在提交后异步发出。

### 下一切片：P3（增值功能）

- 通知接入 UI（未读红点、新信提示）
- 冷启动内容扩充（更多示范信 + 首页匿名用户故事）
- 继续通信 Conversation（P1 表已建）
- 数据导出、手写字体自托管、next 安全补丁升级
- 自动化测试（领取抢占并发）、Sentry
