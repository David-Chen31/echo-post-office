# 回声邮局 · 下一步开发计划

## 主题：夜间主题收尾 + 其他页面的逻辑与美术

> 上一轮把写信页做成了"真正写信"的样子（可编辑称谓署名日期、昼夜暖光、雨声、钢笔）。
> 本轮把这套水准**铺到其余每一页**，并先修掉夜间模式没收尾的问题。
> 一句话目标：**任意一页，白天温暖、夜里孤独暖光，都像在一处安静的邮局里，而不是一个 App。**

---

## 〇、本轮触发（用户反馈）

1. **夜间模式没做完**：写信页夜里只有信纸后面暗，**顶栏和底部「封好信件」按钮那条仍是亮的**，割裂。
2. **关注其他页面**：门厅 / 登录 / 读信 / 信箱 / 拆信 / 回信 / 设置 / 规则 的**逻辑与美术**都要打磨到同一水准。

---

## 一、P0 · 夜间主题收尾（先修这个回归）

### 问题定位
- `WritingAmbience` 的暗色只是一层 `fixed -z-10` 背景，**只盖住信纸周围的空白**。
- 但这些 chrome 仍写死浅色，夜里突兀：
  - 顶栏 `BackHeader`：`bg-paper/90`（`components/ui.tsx:25`）
  - 写信底部按钮栏：`bg-paper/95`（`write/page.tsx` 固定底栏）
  - 底部导航 `BottomNav`：`bg-paper/95`
- 昼夜 / 雨声控件**只在写信页**；读信、拆信这些"该有情绪"的页面完全没有。

### 决策：全站主题（已定 A）
`<html data-theme="day|night">` + 把调色板换成 **CSS 变量令牌**，昼夜切换只改变量，所有页面 chrome 自动适配；昼夜按本地时间自适应 + 全站统一手动开关（右缘悬浮）。"夜里读一封信"同样有氛围。

### 关键要求：真实夜景，不是"简单变深色"（用户明确）
现在的夜=一块暗棕底，没有"天黑了"的感觉。要做成**真实夜景**：
- **冷暖对比**：四周是**冷调靛蓝夜色**（不是暖棕），信纸被**台灯暖光**照亮形成一圈暖光池——冷暗 + 暖光的反差才是"夜"。
- **夜空元素**：极淡的**星点**（少量缓慢闪烁）、一轮**柔光的月**（右上、带光晕）。
- **层次**：顶部夜空稍亮 → 下方房间更暗；四周 vignette 渐暗。
- 信纸始终是被照亮的暖白（阅读焦点）。
- 白天则是温暖窗光的柔和场景。

### 待办（按 A）
- [ ] `tailwind.config` 增暗色语义色（或用 CSS 变量令牌：`--c-paper/--c-letter/--c-ink/--c-line…`，昼夜切换只改变量）。
- [ ] `ThemeProvider`（`data-theme` on html + localStorage + 按小时自适应），替代现在写信页私有的昼夜逻辑。
- [ ] `BackHeader` / `BottomNav` / 各固定底栏改用语义色，夜里自动转深暖。
- [ ] 昼夜 + 雨声控件上移为全站（如统一放右上或「我的」里）。
- **验收**：任意页切到夜晚，**顶栏、正文区、底栏、导航整体统一为暖暗色**，无亮条；信纸仍是被台灯照亮的暖白。

---

## 二、跨页设计系统：把"App 味"统一换成"书信/纸感"

> 现在多页还在用**药丸 Tab、`.field` 方框输入、通用卡片**——这些最像 App。抽一套书信组件，逐页替换。

- [ ] `LetterSheet`：统一的信纸容器（纸纹 + 可选横线 + 抬头/落款位），写信/读信/回信/拆信共用。
- [ ] 纸感输入 `InkField`：去掉方框描边，改**下横线**（像在表格线上写字），聚焦时线变暖。替换 `settings`、`auth` 的 `.field`。
- [ ] `StampTag`：主题/状态标签从灰药丸换成**小邮票/印章**质感（暗红描边、锯齿边）。
- [ ] 空/加载/错误态：`EmptyState`/`Spinner`/`Toast` 文案与视觉再纸感一点（已有基础，统一口吻）。
- **验收**：全站不再出现灰底药丸 Tab 与方框 input；操作按钮保留但描边更弱、更像信封封条。

---

## 三、逐页 · 逻辑 + 美术清单

### 3.1 门厅 Foyer（未登录首页 `page.tsx` Foyer）
- 逻辑：OK。
- 美术：第一印象页，目前只有动画+文案+一个按钮。加**轻昼夜氛围**、把 3 条匿名故事作为"贴在墙上的信"轻展示（现在故事只在登录后书桌）。
- 验收：未登录首屏就能感到"这是一处安静的邮局"，且仍只有一个「进来写信」入口。

### 3.2 登录 / 注册 auth
- **逻辑（要改）**：注册流程别扭——注册成功后要**再点一次获取验证码、切到登录、再输一遍码**（`auth/page.tsx:49-57`）。改为**注册即登录**（后端注册直接签发令牌，或注册成功后用当前码自动登录）。
- 美术：纯表单很 App。做成"在门口登记 / 敲门"的书信感；验证码输入信笺化；文案更暖。
- 验收：注册后一步进入邮局，无需二次输码；页面像"门厅登记处"而非登录表单。

### 3.3 读信 read
- 逻辑：换一封 / 领取 / 24h 提示 OK；**「举报」现在是裸文字链**（`read/page.tsx:121`），收进更含蓄的入口。
- 美术：加"从待回信箱**取出一封信**"的开场（信封→抽出信纸）；跟随全站昼夜；「换一封/领取」按钮弱描边化。
- 验收：读信像拆开一封陌生来信；夜里有暖光氛围。

### 3.4 信箱 mailbox
- **逻辑（潜在 bug）**：草稿点开跳 `/write`，但写信页读的是 **localStorage 草稿**而非该 `draftId` 的服务端草稿（`mailbox/page.tsx:127` → `/write`）——多草稿会对不上。要么按 `draftId` 载入，要么明确单草稿。
- 美术：**收到**已信封化；**寄出 / 回复中 / 收藏 / 草稿**仍是 App 卡片 + 主题标签 + 药丸 Tab。整体"信格 / 抽屉"化，Tab 换纸签。
- 验收：四个分页视觉统一为书信抽屉；草稿续写载入正确的那一封。

### 3.5 拆信 received/[id]
- 逻辑：拆信自动已读 OK。
- 美术：拆信动效有；**反馈是一排 5 枚药丸 + 收藏按钮**，偏表单——改为更含蓄的"读后轻轻放下"（少量、纸感）。
- 验收：读完回信的反馈动作克制、不像打分表单。

### 3.6 回信 reply
- 逻辑：原信可折叠、done 态 OK。**是否也让回信人可署名**（与写信一致，而非固定"亲爱的你/落款"）？——建议给一个可选署名。
- 美术：引导语现在是 `bg-paper2` App 卡片（`reply/page.tsx:125`），改为信纸内的**淡墨小字提示**；跟随昼夜。
- 验收：回信页与写信页同一套书信语言。

### 3.7 设置「我的」settings
- **逻辑**：决策 A 后写信已不选主题，**兴趣匹配被降级**，设置里仍让用户勾 12 个主题（`settings/page.tsx:76`）意义变弱——精简或移除，避免"无意义字段"重现。
- 美术：纯 `.field` + 药丸，很 App。做成**个人信笺 / 铭牌**：昵称像信封落款，等级/信任分/配额用更温柔的呈现。
- 验收：设置页像"我的信笺资料"，无孤立表单感；无低价值字段。

### 3.8 规则 rules
- 逻辑：OK。
- 美术：卡片列表尚可，统一成**一页须知信笺**（编号手写、纸纹），紧急信息块保留醒目。
- 验收：像邮局墙上贴的一纸约定。

### 3.9 投递 sent — 上一轮已做寄信仪式，本轮仅随全站昼夜适配。

---

## 四、氛围延伸

- [ ] 昼夜 + 雨声跟随**全站主题**（第一章 A），在读信 / 拆信 / 回信同样可用——"夜里读一封陌生人的信"是这个产品最动人的时刻。
- [ ] 各页 `LetterPaper` 在夜里由台灯暖光照亮（信纸暖白、周边暗）。

---

## 五、逻辑缺陷单修（穿插进相关切片）

- [ ] 草稿续写按 `draftId` 载入（3.4）。
- [ ] 注册即登录（3.2）。
- [ ] 设置页兴趣主题字段取舍（3.7）。
- [ ] 回信可选署名（3.6，如采纳）。

---

## 六、执行顺序（纵向切片，每片 tsc + build 全绿后提交）

1. **切片一 · 全站主题**：CSS 变量令牌 + ThemeProvider + chrome 适配 + 昼夜/雨声上移。修掉 P0 夜间割裂。`feat(theme):`
2. **切片二 · 书信设计系统**：`LetterSheet` / `InkField` / `StampTag` 抽象 + 替换各页药丸/方框/卡片。`refactor(craft):`
3. **切片三 · 门厅 + 登录**：门厅氛围 + 注册即登录 + 登录信笺化。`feat(craft):`
4. **切片四 · 读信 + 拆信**：取信开场 + 反馈克制 + 夜氛围。`feat(craft):`
5. **切片五 · 信箱 + 回信 + 设置 + 规则**：信格化 + 草稿续写修复 + 设置信笺化 + 规则一纸。`feat(craft):`

> 建议一片一提交、一片一自查截图（尤其夜间各页 chrome）。

---

## 七、不在本轮范围

- 后端治理 / 审核 / 举报 / 会话安全（已完成，不动）。
- 继续通信 Conversation、数据导出、自动化测试、Sentry、`next` 补丁升级 —— 体验轮之后单独排期。

---

## 八、进度追踪（随开发更新）

> 分支延续 `feat/foyer-craft`。每片 `tsc --noEmit` + `next build` 全绿后提交。

- [ ] 切片一 全站主题（修 P0 夜间割裂）
- [ ] 切片二 书信设计系统
- [ ] 切片三 门厅 + 登录
- [ ] 切片四 读信 + 拆信
- [ ] 切片五 信箱 + 回信 + 设置 + 规则
